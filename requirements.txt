# ADTC 2026 Pipeline — base dependencies
#
# IMPORTANT: torch CPU-only, explicitly. Without this line, pip
# installs the GPU build of PyTorch by default + ~2.7 GB of
# CUDA libraries (nvidia-*) — completely useless on the consumer laptop
# with "no dedicated GPU" targeted by the contest (specification §1), and it
# bloats the environment by 5+ GB for nothing. Verified in practice on this
# project: installing from this index brings the venv down from 5.4 GB to 2.0 GB
# (torch==...+cpu, zero nvidia-* package, torch.cuda.is_available() == False).
--extra-index-url https://download.pytorch.org/whl/cpu
torch

ollama
chromadb
# faiss-cpu        # alternative to chromadb for vector search
sentence-transformers
numpy
pandas
pillow
streamlit          # optional, if a web interface is chosen
